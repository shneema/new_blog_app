<%= form_for comment, remote: true do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.text_field :comment, placeholder: 'Say Something........', class: 'form-control' %>
  </div>  
  
  
  <%= form.hidden_field :portfolio_id, value: portfolio.id %>
  
  <div class="actions">
    <%= form.submit 'Submit', class: 'btn btn-primary float-right' %>
  </div>
<% end %>