<%# debugger %>
<% if portfolio.published == false %>
  <%# link_to 'Publish', admin_portfolio_path(portfolio, value: 'publish'), class: 'btn btn-success', remote: true, method: :patch %>
  <%= published_unpublished(portfolio, 'publish', 'success') %>
<% else %>  
  <%# link_to 'UnPublish', admin_portfolio_path(portfolio, value: 'unpublish'), class: 'btn btn-danger', remote: true, method: :patch %>
  <%= published_unpublished(portfolio, 'unpublish', 'danger') %>
<% end %>
